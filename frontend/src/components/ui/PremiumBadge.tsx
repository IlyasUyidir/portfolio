import React from 'react';

export const PremiumBadge: React.FC = () => {
  return (
    <span className="inline-flex items-center justify-center px-2 py-0.5 ml-2 text-xs font-bold text-bg-base bg-primary rounded-full">
      PRO
    </span>
  );
};
